import {createEvent} from "effector";

export const mainBannerTopRequest = createEvent<string>();
export const mainBannerMiddleRequest = createEvent<string>();
export const mainBannerBotRequest = createEvent<string>();